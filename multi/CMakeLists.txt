cmake_minimum_required(VERSION 3.0 )
project( multiTarget )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( multiTarget multiTracking.cpp Roomba.cpp)
target_link_libraries( multiTarget ${OpenCV_LIBS})
